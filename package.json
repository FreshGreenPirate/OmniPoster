{
  "name": "omniposter",
  "private": true,
  "packageManager": "pnpm@8.15.5",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:worker": "pnpm --filter worker dev",
    "build": "pnpm lint && pnpm test && pnpm --filter web build && pnpm --filter worker build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "pnpm -r format"
  },
  "devDependencies": {
    "@types/node": "20.11.30",
    "typescript": "5.4.5"
  }
}
